version: "3.9"

services:
  # Vite Frontend
  frontend:
    build:
      context: .
      dockerfile: dockerfile
      args:
        MODE: production
    image: gauravsingh1998/sinu-frontend:${FRONTEND_TAG:-latest}
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/production/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/production/conf.d:/etc/nginx/conf.d
      - ./ssl/production:/etc/nginx/ssl
      - ./nginx/logs:/var/log/nginx
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      VITE_API_URL: ${VITE_API_URL:-/api}
      VITE_APP_NAME: ${VITE_APP_NAME:-Sinu}
    networks:
      - sinu-network
    healthcheck:
      test: ["CMD", "curl", "-k", "-f", "https://localhost"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Backend API
  api:
    image: gauravsingh1998/sinu-api:${API_TAG:-latest}
    restart: always
    expose:
      - "3000"
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      MONGO_URI: ${MONGO_URI:-mongodb://mongo:27017/sinu-production}
      PORT: 3000
      CORS_ORIGIN: ${CORS_ORIGIN:-https://yourdomain.com}
    depends_on:
      mongo:
        condition: service_healthy
    networks:
      - sinu-network
    deploy:
      replicas: 2
      restart_policy:
        condition: on-failure

  mongo:
    image: mongo:7
    restart: always
    volumes:
      - mongo_data_production:/data/db
      - ./mongo-backup:/backup
    environment:
      MONGO_INITDB_DATABASE: sinu-production
    command: [--auth]
    networks:
      - sinu-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 10s
      retries: 5

  # Optional: Redis cache for production
  redis:
    image: redis:alpine
    restart: always
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    networks:
      - sinu-network

networks:
  sinu-network:
    driver: bridge

volumes:
  mongo_data_production:
  redis_data: